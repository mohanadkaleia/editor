{
  "name": "editor-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "web",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "build:editor": "npm run build -w kurrasah",
    "test:editor": "npm run test -w kurrasah"
  }
}
